# arch-linux-install-scripts

usage:

```shell
curl -fsSL https://7att1ce.github.io/arch-linux-install-scripts/$(curl -fsSL https://7att1ce.github.io/arch-linux-install-scripts/ | grep <number>) | bash
```
